# 安全机制效能评估报告

## 1. 评估目标

量化本系统安全机制引入的性能开销与防御有效性，回答两个核心问题：
1. **成本问题**：添加安全机制后，系统运行增加了多少延迟和经济成本？
2. **有效性问题**：安全机制在多少攻击场景下成功拦截了恶意行为？

---

## 2. 延迟开销分析

### 2.1 认证三阶段延迟构成

基于 2v2×3 轮 fullflow 实测数据（6 组完整审计）：

| 阶段 | 安全功能 | 均值(s) | P50(s) | P95(s) | 最大值(s) | 占比 |
|------|----------|---------|--------|--------|-----------|------|
| **Auth (T4)** | DID 解析 + VP 验签 + nonce 防重放 | 3.013 | 0.760 | 7.833 | 8.049 | 21.3% |
| **Probe (T8)** | 就绪性探测 + 工具链验证 + 负载评估 | 10.407 | 11.290 | 11.679 | 11.730 | 73.6% |
| **Context (T12)** | 上下文哈希一致性校验 | 0.728 | 0.710 | 0.902 | 0.925 | 5.1% |
| **全流程** | 三阶段总计 | 14.149 | 12.696 | 20.090 | 20.341 | 100% |

### 2.2 各阶段开销构成分析

```
Auth (21.3%)  ████████░░░░░░░░░░░░  主要开销：链上 DID 文档解析 (~2s, 取决于 RPC 响应速度)
Probe (73.6%) ████████████████████  主要开销：LLM 推理 (~8s) + 工具调用执行 (~2s)
Context (5.1%) █░░░░░░░░░░░░░░░░░░  主要开销：本地哈希计算 (毫秒级)
```

**关键发现**：
- **身份认证 (Auth)** 的安全成本为 ~3s，其中 DID 链上解析占 60-70%。首轮较慢（需从链上首次获取 DID 文档），后续轮次通过缓存降至 ~0.7s
- **就绪性探测 (Probe)** 占总延迟 73.6%，但其主要开销是 LLM 推理，属于**功能成本而非安全成本**
- **上下文校验 (Context)** 的纯安全开销仅 ~0.7s（SHA-256 哈希计算），效率极高

### 2.3 安全机制的净开销

剥离功能性开销后，纯安全机制增加的延迟：

| 安全机制 | 净延迟开销 | 说明 |
|----------|------------|------|
| ECDSA 签名验证 | ~0.1s | VP 签名验签（纯计算） |
| DID 链上解析 | ~2s（首次）/ ~0.1s（缓存后） | 通过 RPC 查询 ERC-1056 |
| Nonce 防重放 | <0.01s | UUID 生成与比对 |
| 上下文哈希校验 | ~0.7s | SHA-256 计算 + 网络往返 |
| CID 内容校验 | ~0.001s | 本地 SHA-256 重算 |
| **合计** | **~2.8s**（首次）/ **~0.9s**（缓存后） | 占全流程 6-20% |

> **结论**：安全机制的净延迟开销为 0.9~2.8 秒，在 14 秒的全流程中占比 6~20%。LLM 推理（~10s）才是主要瓶颈，安全机制的延迟开销处于可接受范围。

### 2.4 MCP 工具调用延迟分析

基于 MCP 互操作测试的实测数据（`get_current_time` × 20 次连续调用）：

| 指标 | 值 | 说明 |
|------|------|------|
| 平均值 | **2.14 ms** | 毫秒级延迟，对全流程无感 |
| P50 | 2.01 ms | 中位数非常稳定 |
| P95 | 3.34 ms | 尾部延迟可控 |
| 最大值 | 3.34 ms | 无显著毛刺 |
| 最小值 | 1.80 ms | - |

MCP 工具单次调用延迟对比：

| 工具 | 延迟 | 类型 | 分析 |
|------|------|------|------|
| `get_current_time` | 3 ms | 本地计算 | stdio 管道通信开销可忽略 |
| `fetch` | 3,074 ms | 网络 I/O | 瓶颈在外部网络请求，非 MCP 协议层 |

> **关键发现**：MCP stdio 协议层的延迟开销仅 ~2ms，与认证阶段的安全机制开销（0.9~2.8s）相比可忽略不计。工具调用的实际延迟取决于工具自身的操作性质（本地计算 vs 网络 I/O）而非 MCP 协议。

---

## 3. 经济成本分析

### 3.1 链上操作 Gas 成本（Sepolia 实测）

| 操作 | Gas 消耗 | 成本(ETH) | 成本(USD) | 成本(CNY) |
|------|----------|-----------|-----------|-----------|
| reportMisbehavior | 28,977 | 3.20e-08 | 0.0001 | 0.0007 |

> 汇率口径：1 ETH = 2930.00 USD, 1 USD = 7.20 CNY

### 3.2 L2 成本估算（按操作类别）

基于 Sepolia 实测 Gas 数据外推到三大 L2 网络：

| 操作 | 频次 | Base(CNY/笔) | Optimism(CNY/笔) | Arbitrum(CNY/笔) |
|------|------|-------------|------------------|-----------------|
| 注册 Agent | 2 | 0.2405 | 0.2025 | 0.1013 |
| 更新元数据 | 2 | 0.1392 | 0.1350 | 0.0675 |
| 举报不当行为 | 1 | 0.0816 | 0.0966 | 0.0483 |
| 罚没 | 1 | 0.1582 | 0.1477 | 0.0738 |
| 冻结 | 1 | 0.1202 | 0.1224 | 0.0612 |
| 解冻 | 1 | 0.1202 | 0.1224 | 0.0612 |
| 申诉 | 1 | 0.1456 | 0.1392 | 0.0696 |
| 恢复 | 1 | 0.1329 | 0.1308 | 0.0654 |

**在 Arbitrum 上，完整治理周期（注册+举报+罚没+恢复）总成本约 0.29 CNY。**

### 3.3 大规模运行经济测算（线性外推）

| 目标规模 | 总时延估算(s) | 链上总成本(CNY) |
|----------|--------------|----------------|
| 100 agents | ~1,799s | ~0.034 |
| 500 agents | ~8,994s | ~0.170 |
| 1000 agents | ~17,987s | ~0.340 |

> 经济成本不构成系统瓶颈。即使在 1000 Agent 规模下，总治理成本不到 1 元人民币（Arbitrum 口径）。

---

## 4. 安全机制有效性评估

### 4.1 负例测试拦截统计

基于 fullflow_tests 自动化测试结果 (case_assertions.csv)：

| 攻击场景 | 用例 ID | 预期行为 | 实际结果 | 拦截 |
|----------|---------|----------|----------|------|
| 伪造签名认证 | `fake_signature_auth` | HTTP 401 拒绝 | HTTP 401 | ✅ |
| VP Challenge 篡改 | `tampered_vp_challenge` | verify_vp=False | verify_vp=False | ✅ |
| VP 签名篡改 | `tampered_vp_signature` | verify_vp=False | verify_vp=False | ✅ |
| 上下文失配 | `context_mismatch` | context_result=False | context_result=False | ✅ |

**自动化负例拦截率：4/4 = 100%**

### 4.2 治理机制有效性

| 治理动作 | 用例 ID | 预期行为 | 实际结果 | 有效 |
|----------|---------|----------|----------|------|
| 证据 CID 存证 | `evidence_cid_roundtrip` | roundtrip 一致 | 一致 | ✅ |
| 链上举报 | `sepolia_report_misbehavior` | 交易成功 | status=1 | ✅ |
| 本地完整治理 | `local_full_action_set` | 6 步全部通过 | 全部通过 | ✅ |

**治理有效性：3/3 = 100%**

### 4.3 正例通过率

| 指标 | 数值 |
|------|------|
| 正向审计总数 | 6 |
| 正向审计通过数 | 6 |
| 正向通过率 | **100%** |
| 覆盖能力 ID 数 | 13 |
| 用例总数 | 21 |
| 用例通过数 | 21 |
| 用例通过率 | **100%** |

### 4.4 MCP 互操作用例覆盖

| 测试维度 | 用例数 | 通过数 | 通过率 |
|----------|--------|--------|--------|
| 连接性 (stdio) | 2 | 2 | 100% |
| 工具发现 (tools/list) | 2 | 2 | 100% |
| 工具调用 | 2 | 2 | 100% |
| 兼容性 (resources) | 2 | 2 | 100% |
| 权限控制正例 | 1 | 1 | 100% |
| 权限控制负例 | 2 | 2 | 100% |
| 延迟统计 | 1 | 1 | 100% |
| **合计** | **12** | **12** | **100%** |

---

## 5. 互操作性评估

### 5.1 跨角色交互验证

fullflow 测试中验证了以下跨角色交互路径：

```
Verifier C → Holder A (agent_c_op → agent_a_op) : 3轮 ✅
Verifier D → Holder B (agent_d_op → agent_b_op) : 3轮 ✅
```

两条交互路径使用不同的密钥对和 DID，证明系统在标准 DID/VC 协议下支持**任意合法 Agent 间**的互认证。

### 5.2 协议标准合规性

| 标准 | 实现状态 | 说明 |
|------|----------|------|
| W3C DID Core | ✅ | did:ethr:sepolia:0x... 格式 |
| W3C VC Data Model | ✅ | VP/VC 结构含 proof/challenge |
| ERC-1056 | ✅ | 链上 DID 注册表 |
| EIP-712 | ✅ | 结构化签名 |
| **MCP (Model Context Protocol)** | ✅ | stdio 传输层 + JSON-RPC 2.0 消息封装 |
| **A2A (Agent-to-Agent)** | ✅ | Agent Card 发现 + 任务管理 |

### 5.3 MCP 互操作能力

| 能力 | 测试结果 | 条件 |
|------|----------|------|
| stdio Server 启动与握手 | ✅ 2/2 | mcp-server-time, mcp-server-fetch |
| 工具自动发现 (tools/list) | ✅ 2/2 | 3 个工具自动识别 |
| 工具的真实调用 | ✅ 2/2 | get_current_time, fetch |
| 不支持的端点优雅降级 | ✅ 2/2 | resources/list 返回空列表 |
| Toolset VC 权限控制 | ✅ 3/3 | 正例+两类负例全通过 |

---

## 6. 效能评估总结

| 维度 | 指标 | 值 | 评价 |
|------|------|----|------|
| **延迟开销** | 安全机制净延迟 | 0.9~2.8s | ✅ 可接受（占全流程 6~20%） |
| **MCP 协议开销** | MCP stdio 工具调用延迟 | ~2.14ms (avg) | ✅ 可忽略不计 |
| **经济成本** | 单次治理成本(Arbitrum) | ~0.29 CNY | ✅ 极低 |
| **攻击拦截** | 负例拦截率 | 100% (4/4) | ✅ 完全拦截 |
| **MCP 权限控制** | MCP 工具权限拦截率 | 100% (3/3) | ✅ 完全拦截 |
| **治理有效性** | 治理流程通过率 | 100% (3/3) | ✅ 闭环验证 |
| **正例可用性** | 正向通过率 | 100% (6/6) | ✅ 安全机制不影响正常交互 |
| **MCP 互操作** | MCP 用例通过率 | 100% (12/12) | ✅ 全面兼容 |
| **互操作性** | 跨角色互认证 | 2 条路径通过 | ✅ 标准协议互操作 |

> **核心结论**：本系统的安全机制以约 6~20% 的延迟开销和极低的经济成本（<0.3 CNY/治理周期），实现了对 4 类运行时攻击的 100% 拦截，同时不影响正常交互的可用性。MCP 外部工具接入层的协议开销仅 ~2ms，通过 Toolset VC 机制实现了工具级、动作级、资源级的三维权限控制，拦截率 100%。系统的首要性能瓶颈是 LLM 推理（~10 秒），而非安全机制或 MCP 协议层本身。
